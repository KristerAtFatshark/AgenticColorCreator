# TODO

## CFListTreeView

- Move `ReferenceEqualityComparer.cs` to shared space in toolscommon.
- Convert `CFListTreeViewIconMap.cs` to use the Shared-only data `KnownTypes`, for example `{ KnownTypes.StateMachine, "icon-cogs" }`.
- Adapt the current preview data instance, `PreviewCFListTreeItem.cs`, to work like `ContentBrowserTreeViewDisplayControl.xaml` in Charlie's branch.
